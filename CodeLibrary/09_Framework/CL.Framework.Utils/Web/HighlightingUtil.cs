using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CL.Framework.Utils
{
    public class HighlightingUtil
    {
        //    public static Regex keyWords = new Regex(@"xml|version|encoding|request|id|name|parameters|parameter|type|result|response|xml|ta|
        //                                                File|File|Type|CreateTime|FileNo|Priority|List|ProductId|ProductName|OrderCount|FactCount|Code|TID|Bill|
        //                                                Task|Task|Info|Product|Content|Degree|Specification|MakeInfo|BatchNumber|ProductionDate|Qualityperiod|Data|CurCode|CurTID|ParentCode|Layer|ProductionLineId|ProductionGroupId|TaskDate|PlanAmount|RK|RKI|");

        //    public static void SyntaxHighlighting(TextBox rtxt)
        //    {
        //        int selPos = rtxt.SelectionStart;
        //        foreach (Match keyWordMatch in keyWords.Matches(rtxt.Text))
        //        {

        //            rtxt.Select(keyWordMatch.Index, keyWordMatch.Length);
        //            rtxt.SelectionColor = Color.Blue;
        //            rtxt.SelectionStart = selPos;
        //            rtxt.SelectionColor = Color.Black;
        //        }
        //    }
    }
}
